# Project Key (required)
sonar.projectKey=devstack-component-catalog

# Project Name (optional)
sonar.projectName=devstack-component-catalog

# Comma-separated paths to directories with sources (required)
sonar.sources=src/main/java

# Encoding of the source files (optional but recommended as default is ASCII)
sonar.sourceEncoding=UTF-8

# TODO BB client package temporary excluded until after removing unneeded generated BB REST client classes
sonar.exclusions=src/main/java/com/boehringer/componentcatalog/client/bitbucket/v89/**,src/main/java/com/boehringer/componentcatalog/server/api/**,src/main/java/com/boehringer/componentcatalog/server/model/**
sonar.coverage.exclusions=src/main/java/com/boehringer/componentcatalog/client/bitbucket/v89/**

sonar.tests=src/test/java

# Plugin-specific settings
sonar.java.binaries=build/classes
sonar.java.libraries=build/dependencies/*.jar,docker/app.jar
sonar.junit.reportPaths=build/test-results/test
sonar.coverage.jacoco.xmlReportPaths=build/reports/jacoco/test/jacocoTestReport.xml
